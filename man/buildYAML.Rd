% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/buildYAML.R
\name{buildYAML}
\alias{buildYAML}
\title{Build Quarto YAML and prepare project structure}
\usage{
buildYAML(
  build_dir = "_build",
  publish_dir = "_publish",
  index_filename = "index.qmd",
  quarto_filename = "_quarto.yml",
  language = "EN",
  output_format = c("html"),
  extensions = c("spl", "bst", "cls", "md", "aux", "log", "tex", "jpg", "sty", "docx",
    "pdf", "html"),
  render = TRUE
)
}
\arguments{
\item{build_dir}{Directory where package support files are copied (created if needed).}

\item{publish_dir}{Output directory configured in the generated \verb{_quarto.yml} (re-created).}

\item{index_filename}{Report entrypoint (e.g. \code{index.qmd}). Used to invoke Quarto downstream.}

\item{quarto_filename}{Target filename for the generated Quarto config (default \verb{_quarto.yml}).}

\item{language}{Language code (e.g. \code{"EN"}, \code{"ES"}) to pick suffixed files like \verb{_html_ES.yml}.}

\item{output_format}{Character vector of output formats to include (e.g. \code{c("html","pdf")}).}

\item{extensions}{Reserved for future cleanup behavior (currently used only on error paths).}

\item{render}{Ignored. Rendering should be done via Quarto after this function returns.}
}
\description{
Assemble a consolidated Quarto configuration (\verb{_quarto.yml}) by merging project files
(parameters, authors, format-specific YAMLs) with language-specific variants when present.
Copies package support files into \code{build_dir}, ensures a clean \code{publish_dir}, and writes
the final \verb{_quarto.yml}. Rendering is not performed; use Quarto CLI or
\code{quarto::quarto_render()} after calling this function.
}
